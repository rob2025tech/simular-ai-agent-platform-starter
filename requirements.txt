fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
pyyaml>=6.0
httpx>=0.27
python-dotenv>=1.0

# --- optional, per vendor (install only what you use) ---
# ollama / groq / openai / openrouter / together  -> httpx only, no SDK needed
# fastembed
# chromadb
# qdrant-client
# psycopg[binary]
# duckduckgo-search
# langfuse
